{
  "name": "ripple",
  "version": "0.1.0",
  "license": "MIT",
  "bin": {
    "ppx": "./bin/ppx"
  },
  "scripts": {
    "build": "bsb",
    "test": "bsb && mocha lib/js/test/*.js lib/js/test/ripple/*.js",
    "postinstall": "bsb && mkdir -p lib/ocaml && cp -r lib/bs/src/ripple/* lib/ocaml/"
  },
  "devDependencies": {
    "mocha": "^2.5.3"
  }
}
